version: '3.8'
services:
  linkedin-mcp:
    build: .
    ports:
      - "8000:8000"
    environment:
      - LINKEDIN_COOKIE=${LINKEDIN_COOKIE}
    command: ["--transport", "streamable-http", "--host", "0.0.0.0", "--port", "8000", "--path", "/mcp"]
